问题:
加入金山sdk后
出现错误
解决办法
终端输入
gradlew processDebugManifest --stacktrace 查看详情
这个需要装jdk1.8
装完之后设置环境变量
最终的解决办法是 把项目的最小sdk设置为15 之前是14

直播的頁面 StartLiveActivity

金山sdk git  
https://github.com/ksvc/KSYStreamer_Android

GTV问题
1：c++方面的问题

2：ddos方面的问题
高防和流量清洗，这个不多讲了，大的云平台都有货架产品。
讲一些他们没有的，用户的矩阵话管理，和反侦察。
如何判断vpn,前端可以抓一些用户数据，如果同一个IP大量的不同用户。基本上可以判断是VPN。对于VPN可以单独的分配IP地址。
3：流协议方面的问题
政府对于ts或者rtmp是完全可以做到内容检测的。应对的方法就是内容加密。这个涉及到推流和拉流，都要自己做。

3：数据库方面的
mysql postgre redis
hadoop hdfs

N卡 CUDA 计算库
h264
帧内编码，帧间预测，p,b,sp帧。
帧内编码，帧间预测，熵编码（VCL），封包处理（NAL），网络传输
帧内压缩的原理是，视觉生理冗余（帧内压缩），统计冗余（熵编码），时间冗余（帧间预测）
h265
H.265相比H.264最主要的改变是采用了块的四叉树划分结构，采用了从64x64~8x8像素的自适应块划分，并基于这种块划分结构采用一系列自适应的预测和变换等编码技术。
265的适配不好，目前不建议采用

